import { internalMutation } from './_generated/server';

export default internalMutation({
  handler: async () => {
    // const anyArena = await ctx.db.query('arenas').first();
    // if (anyArena) return;
    // await Promise.all(arenas.map(arena => ctx.db.insert('arenas', arena)));
  }
});
